#ifndef UTILS_H
#define UTILS_H
// Dùng ifndef để đảm bảo chỉ load file này 1 lần không lặp lại

#include <iostream>
#include <stdint.h>
#include <stdlib.h>
using namespace std;

int rand_num(int min, int max);


#endif